import { useState } from 'react';
function Module9() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>9. ReactJS-HOL Count: {count}</button>;
}
export default Module9;
